const interfaceConfig = {
    // Application name
    APP_NAME: 'Jitsi Meet',

    // Default language
    DEFAULT_LANGUAGE: 'en',

    // Enable/disable the film strip
    FILM_STRIP: {
        ENABLED: true,
        MAX_HEIGHT: 120,
    },

    // Enable/disable the toolbar
    TOOLBAR_BUTTONS: [
        'microphone', 'camera', 'hangup', 'chat', 'recording', 'etherpad', 'sharedvideo', 'settings', 'raisehand', 'videoquality', 'filmstrip', 'invite', 'feedback', 'stats', 'shortcuts', 'tileview', 'fullscreen', 'fodeviceselection', 'mute-everyone', 'security'
    ],

    // Enable/disable the welcome page
    WELCOME_PAGE_ENABLED: true,

    // Enable/disable the prejoin page
    PREJOIN_PAGE_ENABLED: true,

    // Enable/disable the lobby feature
    LOBBY_MODE_ENABLED: false,

    // Enable/disable the live streaming feature
    LIVE_STREAMING_ENABLED: false,

    // Enable/disable the recording feature
    RECORDING_ENABLED: false,

    // Enable/disable the video quality settings
    VIDEO_QUALITY: {
        DEFAULT: '720p',
        OPTIONS: ['360p', '480p', '720p', '1080p'],
    },

    // Enable/disable the tile view
    TILE_VIEW_ENABLED: true,

    // Enable/disable the chat feature
    CHAT_ENABLED: true,

    // Enable/disable the feedback feature
    FEEDBACK_ENABLED: true,

    // Enable/disable the stats feature
    STATS_ENABLED: true,
};

export default interfaceConfig;